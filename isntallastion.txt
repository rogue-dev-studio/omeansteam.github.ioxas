SETUP:
install ruby
clone jekyll minima

Masuk ke folder dan jalankan terminal, hunakan kode dibawah:

run localhost:
bundle install
bundle exec jekyll serve

git config --global user.email "aris.hadisopiyan@gmail.com"
git config --global user.name "Aris hadisopiyan"
git config --global user.email "you@example.com"
git config --global user.name "Your Name"